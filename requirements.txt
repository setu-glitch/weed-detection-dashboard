# CPU-only PyTorch wheels. Without this line, pip pulls the CUDA build on
# Linux (~2.5 GB) and most free-tier builds run out of disk.
--extra-index-url https://download.pytorch.org/whl/cpu

streamlit>=1.36,<2.0
pillow>=10.1
pyyaml>=6.0

torch>=2.2,<3
torchvision>=0.17,<1
ultralytics>=8.3.0
